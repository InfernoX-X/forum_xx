<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> | <%= siteTitle %></title>
    <%- include("../partials/variables") %>
    <style>
        :root {
            --surface: var(--px-surface); 
        }

        body { 
            background-color: var(--bg); 
            color: var(--text-main); 
            font-family: var(--font-family); 
            -webkit-font-smoothing: antialiased;
            margin: 0;
        }

        .main-wrapper {
            max-width: 800px;
            margin: 0 auto;
            padding: 24px;
        }

        /* Back Button Styling */
        .nav-header { margin-bottom: 20px; }
        .back-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: var(--text-muted);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.2s;
        }
        .back-btn:hover { color: var(--text-main); }

        /* Hero/Media Section */
        .post-hero-wrapper {
            width: 100%;
            background: #000;
            border-radius: var(--radius);
            overflow: hidden;
            border: 1px solid var(--border);
            margin-bottom: 24px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }
        .post-hero-image {
            width: 100%;
            max-height: max-content;
            object-fit: contain;
            display: block;
        }

        /* Title & Action Section */
        .post-header { margin-bottom: 20px; }
        .post-header h1, .post-header h1 a {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 12px;
            line-height: 1.3;
            color: var(--white);
            text-decoration: none;
        }
        .post-header h1 a:hover{
            text-decoration: underline;
        }
        /* Author/Channel Bar */
        .author-bar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding-bottom: 20px;
            margin-bottom: 20px;
            border-bottom: 1px solid var(--border);
        }
        .author-info { display: flex; align-items: center; gap: 12px; }
        .avatar-circle {
            width: 40px; height: 40px;
            background: linear-gradient(45deg, #333, #555);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-weight: 700; color: var(--white);
        }
        .author-details div { font-weight: 600; font-size: 1rem; }
        .author-details span { color: var(--text-muted); font-size: 0.85rem; }

        /* Content Area */
        .description-card {
            background: var(--surface);
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 40px;
        }
        .description-card:hover { background: #252525; }
        
        /* Container for the content */
        #content-wrapper {
            overflow: hidden;
            position: relative;
            transition: max-height 0.3s ease-out;
        }

        /* Clamping the text to ~4 lines when collapsed */
        .content-collapsed {
            max-height: 100px; /* Adjust this height as needed */
            -webkit-mask-image: linear-gradient(to bottom, black 50%, transparent 100%);
            mask-image: linear-gradient(to bottom, black 50%, transparent 100%);
        }

        /* Expanded state (no mask) */
        .content-expanded {
            max-height: 2000px !important; /* Large enough to fit any text */
            -webkit-mask-image: none !important;
            mask-image: none !important;
        }

        /* Read More Button Styling */
        .read-more-link {
            background: none;
            border: none;
            color: var(--accent);
            padding: 0;
            margin-top: 8px;
            font-weight: 700;
            font-size: 0.9rem;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .read-more-link:hover {
            color: var(--accent-hover);
            text-decoration: underline;
        }


        .tags-row { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 16px; }
        .forum-badge {
            background: var(--accent);
            color: #fff;
            font-size: 0.75rem;
            padding: 4px 10px;
            border-radius: 6px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .post-content {
            font-size: 1.05rem;
            line-height: 1.6;
            color: #eee;
        }

        /* External Link Styling */
        .link-footer {
            display: flex;
            align-items: center;
            gap: 12px;
            background: rgba(74, 144, 226, 0.1);
            padding: 16px;
            border-radius: 12px;
            border: 1px dashed #4A90E2;
        }
        .link-footer a {
            color: #4A90E2;
            text-decoration: none;
            font-weight: 600;
            word-break: break-all;
        }
        .link-footer a:hover { text-decoration: underline; }

        .gallery-grid .thumb-wrapper:hover img {
            transform: scale(1.05);
        }
        
        .post-hero-image {
            transition: opacity 0.2s;
        }
        
        .post-hero-image:hover {
            opacity: 0.9;
        }
        /* Layout Adjustment */
        @media (min-width: 1024px) {
            .main-wrapper {
                max-width: 1200px; /* Wider to accommodate sidebar */
                display: grid;
                grid-template-columns: 1fr 350px;
                gap: 32px;
                align-items: start;
            }
            .left-column { grid-column: 1; }
            .right-sidebar { grid-column: 2; position: sticky; top: 24px; }
        }

        /* Side Recommend List */
        .sidebar-title {
            font-size: 1rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .sidebar-title::before {
            content: '';
            width: 4px;
            height: 18px;
            background: var(--accent);
            border-radius: 2px;
        }

        .rec-list { display: flex; flex-direction: column; gap: 16px; }
        
        .rec-item {
            display: grid;
            grid-template-columns: 100px 1fr;
            gap: 12px;
            text-decoration: none;
            color: inherit;
            background: var(--surface);
            padding: 10px;
            border-radius: 12px;
            border: 1px solid var(--border);
            transition: transform 0.2s, border-color 0.2s;
        }

        .rec-item:hover {
            transform: translateY(-3px);
            border-color: #444;
            background: #222;
        }

        .rec-thumb-box {
            width: 100px;
            height: 75px;
            border-radius: 8px;
            overflow: hidden;
            background: #000;
        }

        .rec-thumb-box img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.8;
        }

        .rec-title {
            font-size: 0.9rem;
            font-weight: 600;
            line-height: 1.4;
            color: var(--text-main);
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .rec-meta { font-size: 0.75rem; color: var(--text-muted); margin-top: 4px; }

        /* Comment Section Layout */
        .comments-container {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid var(--border);
        }

        .comment-form {
            margin-bottom: 32px;
        }

        .comment-input {
            width: 100%;
            background: #000;
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 15px;
            color: var(--text-main);
            font-family: inherit;
            font-size: 1rem;
            resize: vertical;
            min-height: 80px;
            margin-bottom: 12px;
            transition: border-color 0.2s;
        }

        .comment-input:focus {
            outline: none;
            border-color: var(--accent);
        }

        .submit-comment {
            background: var(--accent);
            color: var(--white);
            border: none;
            padding: 10px 24px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
        }

        .submit-comment:hover {
            background: var(--accent-hover);
        }

        /* Comment List */
        .comment-item {
            display: flex;
            gap: 16px;
            margin-bottom: 24px;
        }

        .comment-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: var(--border);
            flex-shrink: 0;
        }

        .comment-body {
            background: var(--surface);
            padding: 14px 18px;
            border-radius: 0 16px 16px 16px; /* Chat bubble style */
            flex-grow: 1;
        }

        .comment-user {
            font-weight: 700;
            font-size: 0.9rem;
            color: var(--accent);
            margin-bottom: 4px;
        }

        .comment-text {
            font-size: 0.95rem;
            line-height: 1.5;
            color: #ddd;
        }

        .comment-date {
            font-size: 0.75rem;
            color: var(--text-muted);
            margin-top: 8px;
        }
    </style>
<body>
<%- include("../partials/navBar") %>

<div class="main-wrapper">
    <div class="left-column">
        <div class="nav-header">
            <a href="/" class="back-btn">← Back to Feed</a>
        </div>
        <% if (images && images.length > 0) { %>
            <div id="posts-feed"> 
                <div class="post-hero-wrapper popUpClass" style="margin-bottom: 12px;">
                    <img src="<%= images[0].image_url %>" loading="lazy" class="post-hero-image" alt="Primary Image" style="cursor: pointer;">
                </div>
    
                <% if (images.length > 1) { %>
                    <div class="gallery-grid" style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 24px;">
                        <% images.slice(1).forEach(img => { %>
                            <div class="thumb-wrapper popUpClass" style="aspect-ratio: 1/1; overflow: hidden; border-radius: 8px; border: 1px solid var(--border); cursor: pointer;">
                                <img src="<%= img.image_url %>" loading="lazy" style="width: 100%; height: 100%; object-fit: cover; transition: transform 0.3s;">
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        <% } %>
        
        <div class="post-header">
            <h1><a href="<%= post.url %>" target="_blank">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path></svg>
                <%= post.title %>
            </a></h1>
            
            <div class="author-bar">
                <div class="author-info">
                    <div class="author-details">
                        <div style="color: var(--accent);"><a style="color: var(--accent); text-decoration: none;" href="/user/<%= post.userId %>"><%= post.username %></a></div>
                        <span><%= timeAgo(post.created_at) %></span>
                    </div>
                </div>
                
            </div>
        </div>
        <div class="description-card">
            <div class="tags-row">
                <% if (post.categories) { %>
                    <% post.categories.split(',').forEach(cat => { %>
                        <span class="forum-badge"><%= cat %></span>
                    <% }) %>
                <% } %>
            </div>
            
            <div id="content-wrapper" class="content-collapsed">
                <p class="post-content">
                    <strong>Description:</strong> <%= post.content %>
                </p>
            </div>

            <button id="read-more-btn" class="read-more-link" style="display: none;">Read More</button>
    
            <% if(post.url) { %>
                <div style="margin-top: 30px;">
                    <div class="link-footer">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#4A90E2" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path></svg>
                        <a href="<%= post.url %>" target="_blank"><%= post.url %></a>
                    </div>
                </div>
            <% } %>
        </div>

        <div class="comments-container">
            <h3 style="margin-bottom: 20px;">Comments</h3>

            <form class="comment-form" action="/forum/post/<%= post.id %>/comment" method="POST">
                <textarea 
                    name="comment" 
                    class="comment-input" 
                    placeholder="Write a thoughtful comment..." 
                    required
                ></textarea>
                <div style="text-align: right;">
                    <button type="submit" class="submit-comment">Post Comment</button>
                </div>
            </form>

            <div class="comments-list">
                <% if (typeof comments !== 'undefined' && comments.length > 0) { %>
                    <% comments.forEach(comment => { %>
                        <div class="comment-item">
                            <!-- <div class="comment-avatar"></div>  -->
                            <div class="comment-body">
                                <div class="comment-user" style="margin-bottom: 8px;"><a style="color: var(--accent); text-decoration: none;" href="/user/<%= comment.userId %>" >@<%= comment.username %></a> <span class="comment-date">● <%= timeAgo(comment.created_at) %></span></div>
                                <div class="comment-text"><%= comment.content %></div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p style="color: var(--text-muted); text-align: center;">No comments yet. Be the first to share your thoughts!</p>
                <% } %>
            </div>
        </div>

    </div>

    <aside class="right-sidebar">
        <h3 class="sidebar-title">Related Content</h3>
        <div class="rec-list">
            <% if (recommended && recommended.length > 0) { %>
                <% recommended.forEach(rec => { %>
                    <a href="/forum/post/<%= rec.id %>" class="rec-item">
                        <div class="rec-thumb-box">
                            <img src="<%= rec.thumb %>" alt="">
                        </div>
                        <div class="rec-details">
                            <div class="rec-title"><%= rec.title %></div>
                            <div class="rec-meta"><%= timeAgo(rec.created_at) %></div>
                        </div>
                    </a>
                <% }) %>
            <% } else { %>
                <div style="background: var(--surface); padding: 20px; border-radius: 12px; border: 1px dashed var(--border); text-align: center; color: var(--text-muted); font-size: 0.8rem;">
                    No related posts found
                </div>
            <% } %>
        </div>
    </aside>
</div>

<%- include("../partials/imageViewer") %>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        const wrapper = document.getElementById('content-wrapper');
        const btn = document.getElementById('read-more-btn');
        
        // Check if the content is taller than our limit (100px)
        if (wrapper.scrollHeight > 100) {
            btn.style.display = 'inline-block'; // Show button only if content is long
        }

        btn.addEventListener('click', function() {
            if (wrapper.classList.contains('content-collapsed')) {
                wrapper.classList.remove('content-collapsed');
                wrapper.classList.add('content-expanded');
                btn.textContent = 'Read Less';
            } else {
                wrapper.classList.remove('content-expanded');
                wrapper.classList.add('content-collapsed');
                btn.textContent = 'Read More';
                
                // Optional: Scroll back up to the top of the description if closing
                wrapper.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            }
        });
    });

    if (typeof initializeImageGallery === "function") {
        document.querySelectorAll(".popUpClass").forEach(el => {
            initializeImageGallery(el); 
        });
        initializeImageGallery("popUpClass");
    }
</script>

</body>
</html>